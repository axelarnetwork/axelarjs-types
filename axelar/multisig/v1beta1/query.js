"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v3.21.12
// source: axelar/multisig/v1beta1/query.proto
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ParamsResponse = exports.ParamsRequest = exports.KeygenSessionResponse = exports.KeygenSessionRequest = exports.KeyResponse = exports.KeygenParticipant = exports.KeyRequest = exports.NextKeyIDResponse = exports.NextKeyIDRequest = exports.KeyIDResponse = exports.KeyIDRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const long_1 = __importDefault(require("long"));
const minimal_1 = __importDefault(require("protobufjs/minimal"));
const timestamp_1 = require("../../../google/protobuf/timestamp");
const types_1 = require("../exported/v1beta1/types");
const params_1 = require("./params");
exports.protobufPackage = "axelar.multisig.v1beta1";
function createBaseKeyIDRequest() {
    return { chain: "" };
}
exports.KeyIDRequest = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.chain !== "") {
            writer.uint32(10).string(message.chain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseKeyIDRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.chain = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { chain: isSet(object.chain) ? gt.String(object.chain) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.chain !== "") {
            obj.chain = message.chain;
        }
        return obj;
    },
    create(base) {
        return exports.KeyIDRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseKeyIDRequest();
        message.chain = (_a = object.chain) !== null && _a !== void 0 ? _a : "";
        return message;
    },
};
function createBaseKeyIDResponse() {
    return { keyId: "" };
}
exports.KeyIDResponse = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.keyId !== "") {
            writer.uint32(10).string(message.keyId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseKeyIDResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.keyId = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { keyId: isSet(object.keyId) ? gt.String(object.keyId) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.keyId !== "") {
            obj.keyId = message.keyId;
        }
        return obj;
    },
    create(base) {
        return exports.KeyIDResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseKeyIDResponse();
        message.keyId = (_a = object.keyId) !== null && _a !== void 0 ? _a : "";
        return message;
    },
};
function createBaseNextKeyIDRequest() {
    return { chain: "" };
}
exports.NextKeyIDRequest = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.chain !== "") {
            writer.uint32(10).string(message.chain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNextKeyIDRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.chain = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { chain: isSet(object.chain) ? gt.String(object.chain) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.chain !== "") {
            obj.chain = message.chain;
        }
        return obj;
    },
    create(base) {
        return exports.NextKeyIDRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseNextKeyIDRequest();
        message.chain = (_a = object.chain) !== null && _a !== void 0 ? _a : "";
        return message;
    },
};
function createBaseNextKeyIDResponse() {
    return { keyId: "" };
}
exports.NextKeyIDResponse = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.keyId !== "") {
            writer.uint32(10).string(message.keyId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNextKeyIDResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.keyId = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { keyId: isSet(object.keyId) ? gt.String(object.keyId) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.keyId !== "") {
            obj.keyId = message.keyId;
        }
        return obj;
    },
    create(base) {
        return exports.NextKeyIDResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseNextKeyIDResponse();
        message.keyId = (_a = object.keyId) !== null && _a !== void 0 ? _a : "";
        return message;
    },
};
function createBaseKeyRequest() {
    return { keyId: "" };
}
exports.KeyRequest = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.keyId !== "") {
            writer.uint32(10).string(message.keyId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseKeyRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.keyId = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { keyId: isSet(object.keyId) ? gt.String(object.keyId) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.keyId !== "") {
            obj.keyId = message.keyId;
        }
        return obj;
    },
    create(base) {
        return exports.KeyRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseKeyRequest();
        message.keyId = (_a = object.keyId) !== null && _a !== void 0 ? _a : "";
        return message;
    },
};
function createBaseKeygenParticipant() {
    return { address: "", weight: Buffer.alloc(0), pubKey: "" };
}
exports.KeygenParticipant = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.address !== "") {
            writer.uint32(10).string(message.address);
        }
        if (message.weight.length !== 0) {
            writer.uint32(18).bytes(message.weight);
        }
        if (message.pubKey !== "") {
            writer.uint32(26).string(message.pubKey);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseKeygenParticipant();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.address = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.weight = reader.bytes();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.pubKey = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            address: isSet(object.address) ? gt.String(object.address) : "",
            weight: isSet(object.weight) ? Buffer.from(bytesFromBase64(object.weight)) : Buffer.alloc(0),
            pubKey: isSet(object.pubKey) ? gt.String(object.pubKey) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.address !== "") {
            obj.address = message.address;
        }
        if (message.weight.length !== 0) {
            obj.weight = base64FromBytes(message.weight);
        }
        if (message.pubKey !== "") {
            obj.pubKey = message.pubKey;
        }
        return obj;
    },
    create(base) {
        return exports.KeygenParticipant.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseKeygenParticipant();
        message.address = (_a = object.address) !== null && _a !== void 0 ? _a : "";
        message.weight = (_b = object.weight) !== null && _b !== void 0 ? _b : Buffer.alloc(0);
        message.pubKey = (_c = object.pubKey) !== null && _c !== void 0 ? _c : "";
        return message;
    },
};
function createBaseKeyResponse() {
    return {
        keyId: "",
        state: 0,
        startedAt: long_1.default.ZERO,
        startedAtTimestamp: undefined,
        thresholdWeight: Buffer.alloc(0),
        bondedWeight: Buffer.alloc(0),
        participants: [],
    };
}
exports.KeyResponse = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.keyId !== "") {
            writer.uint32(10).string(message.keyId);
        }
        if (message.state !== 0) {
            writer.uint32(16).int32(message.state);
        }
        if (!message.startedAt.equals(long_1.default.ZERO)) {
            writer.uint32(24).int64(message.startedAt);
        }
        if (message.startedAtTimestamp !== undefined) {
            timestamp_1.Timestamp.encode(message.startedAtTimestamp, writer.uint32(34).fork()).ldelim();
        }
        if (message.thresholdWeight.length !== 0) {
            writer.uint32(42).bytes(message.thresholdWeight);
        }
        if (message.bondedWeight.length !== 0) {
            writer.uint32(50).bytes(message.bondedWeight);
        }
        for (const v of message.participants) {
            exports.KeygenParticipant.encode(v, writer.uint32(58).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseKeyResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.keyId = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.state = reader.int32();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.startedAt = reader.int64();
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.startedAtTimestamp = timestamp_1.Timestamp.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.thresholdWeight = reader.bytes();
                    continue;
                case 6:
                    if (tag !== 50) {
                        break;
                    }
                    message.bondedWeight = reader.bytes();
                    continue;
                case 7:
                    if (tag !== 58) {
                        break;
                    }
                    message.participants.push(exports.KeygenParticipant.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            keyId: isSet(object.keyId) ? gt.String(object.keyId) : "",
            state: isSet(object.state) ? (0, types_1.keyStateFromJSON)(object.state) : 0,
            startedAt: isSet(object.startedAt) ? long_1.default.fromValue(object.startedAt) : long_1.default.ZERO,
            startedAtTimestamp: isSet(object.startedAtTimestamp)
                ? fromJsonTimestamp(object.startedAtTimestamp)
                : undefined,
            thresholdWeight: isSet(object.thresholdWeight)
                ? Buffer.from(bytesFromBase64(object.thresholdWeight))
                : Buffer.alloc(0),
            bondedWeight: isSet(object.bondedWeight)
                ? Buffer.from(bytesFromBase64(object.bondedWeight))
                : Buffer.alloc(0),
            participants: gt.Array.isArray(object === null || object === void 0 ? void 0 : object.participants)
                ? object.participants.map((e) => exports.KeygenParticipant.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        var _a;
        const obj = {};
        if (message.keyId !== "") {
            obj.keyId = message.keyId;
        }
        if (message.state !== 0) {
            obj.state = (0, types_1.keyStateToJSON)(message.state);
        }
        if (!message.startedAt.equals(long_1.default.ZERO)) {
            obj.startedAt = (message.startedAt || long_1.default.ZERO).toString();
        }
        if (message.startedAtTimestamp !== undefined) {
            obj.startedAtTimestamp = fromTimestamp(message.startedAtTimestamp).toISOString();
        }
        if (message.thresholdWeight.length !== 0) {
            obj.thresholdWeight = base64FromBytes(message.thresholdWeight);
        }
        if (message.bondedWeight.length !== 0) {
            obj.bondedWeight = base64FromBytes(message.bondedWeight);
        }
        if ((_a = message.participants) === null || _a === void 0 ? void 0 : _a.length) {
            obj.participants = message.participants.map((e) => exports.KeygenParticipant.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.KeyResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBaseKeyResponse();
        message.keyId = (_a = object.keyId) !== null && _a !== void 0 ? _a : "";
        message.state = (_b = object.state) !== null && _b !== void 0 ? _b : 0;
        message.startedAt =
            object.startedAt !== undefined && object.startedAt !== null
                ? long_1.default.fromValue(object.startedAt)
                : long_1.default.ZERO;
        message.startedAtTimestamp =
            object.startedAtTimestamp !== undefined && object.startedAtTimestamp !== null
                ? timestamp_1.Timestamp.fromPartial(object.startedAtTimestamp)
                : undefined;
        message.thresholdWeight = (_c = object.thresholdWeight) !== null && _c !== void 0 ? _c : Buffer.alloc(0);
        message.bondedWeight = (_d = object.bondedWeight) !== null && _d !== void 0 ? _d : Buffer.alloc(0);
        message.participants = ((_e = object.participants) === null || _e === void 0 ? void 0 : _e.map((e) => exports.KeygenParticipant.fromPartial(e))) || [];
        return message;
    },
};
function createBaseKeygenSessionRequest() {
    return { keyId: "" };
}
exports.KeygenSessionRequest = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.keyId !== "") {
            writer.uint32(10).string(message.keyId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseKeygenSessionRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.keyId = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { keyId: isSet(object.keyId) ? gt.String(object.keyId) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.keyId !== "") {
            obj.keyId = message.keyId;
        }
        return obj;
    },
    create(base) {
        return exports.KeygenSessionRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseKeygenSessionRequest();
        message.keyId = (_a = object.keyId) !== null && _a !== void 0 ? _a : "";
        return message;
    },
};
function createBaseKeygenSessionResponse() {
    return {
        startedAt: long_1.default.ZERO,
        startedAtTimestamp: undefined,
        expiresAt: long_1.default.ZERO,
        completedAt: long_1.default.ZERO,
        gracePeriod: long_1.default.ZERO,
        state: 0,
        keygenThresholdWeight: Buffer.alloc(0),
        signingThresholdWeight: Buffer.alloc(0),
        bondedWeight: Buffer.alloc(0),
        participants: [],
    };
}
exports.KeygenSessionResponse = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (!message.startedAt.equals(long_1.default.ZERO)) {
            writer.uint32(8).int64(message.startedAt);
        }
        if (message.startedAtTimestamp !== undefined) {
            timestamp_1.Timestamp.encode(message.startedAtTimestamp, writer.uint32(18).fork()).ldelim();
        }
        if (!message.expiresAt.equals(long_1.default.ZERO)) {
            writer.uint32(24).int64(message.expiresAt);
        }
        if (!message.completedAt.equals(long_1.default.ZERO)) {
            writer.uint32(32).int64(message.completedAt);
        }
        if (!message.gracePeriod.equals(long_1.default.ZERO)) {
            writer.uint32(40).int64(message.gracePeriod);
        }
        if (message.state !== 0) {
            writer.uint32(48).int32(message.state);
        }
        if (message.keygenThresholdWeight.length !== 0) {
            writer.uint32(58).bytes(message.keygenThresholdWeight);
        }
        if (message.signingThresholdWeight.length !== 0) {
            writer.uint32(66).bytes(message.signingThresholdWeight);
        }
        if (message.bondedWeight.length !== 0) {
            writer.uint32(74).bytes(message.bondedWeight);
        }
        for (const v of message.participants) {
            exports.KeygenParticipant.encode(v, writer.uint32(82).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseKeygenSessionResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.startedAt = reader.int64();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.startedAtTimestamp = timestamp_1.Timestamp.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.expiresAt = reader.int64();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.completedAt = reader.int64();
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.gracePeriod = reader.int64();
                    continue;
                case 6:
                    if (tag !== 48) {
                        break;
                    }
                    message.state = reader.int32();
                    continue;
                case 7:
                    if (tag !== 58) {
                        break;
                    }
                    message.keygenThresholdWeight = reader.bytes();
                    continue;
                case 8:
                    if (tag !== 66) {
                        break;
                    }
                    message.signingThresholdWeight = reader.bytes();
                    continue;
                case 9:
                    if (tag !== 74) {
                        break;
                    }
                    message.bondedWeight = reader.bytes();
                    continue;
                case 10:
                    if (tag !== 82) {
                        break;
                    }
                    message.participants.push(exports.KeygenParticipant.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            startedAt: isSet(object.startedAt) ? long_1.default.fromValue(object.startedAt) : long_1.default.ZERO,
            startedAtTimestamp: isSet(object.startedAtTimestamp)
                ? fromJsonTimestamp(object.startedAtTimestamp)
                : undefined,
            expiresAt: isSet(object.expiresAt) ? long_1.default.fromValue(object.expiresAt) : long_1.default.ZERO,
            completedAt: isSet(object.completedAt) ? long_1.default.fromValue(object.completedAt) : long_1.default.ZERO,
            gracePeriod: isSet(object.gracePeriod) ? long_1.default.fromValue(object.gracePeriod) : long_1.default.ZERO,
            state: isSet(object.state) ? (0, types_1.multisigStateFromJSON)(object.state) : 0,
            keygenThresholdWeight: isSet(object.keygenThresholdWeight)
                ? Buffer.from(bytesFromBase64(object.keygenThresholdWeight))
                : Buffer.alloc(0),
            signingThresholdWeight: isSet(object.signingThresholdWeight)
                ? Buffer.from(bytesFromBase64(object.signingThresholdWeight))
                : Buffer.alloc(0),
            bondedWeight: isSet(object.bondedWeight)
                ? Buffer.from(bytesFromBase64(object.bondedWeight))
                : Buffer.alloc(0),
            participants: gt.Array.isArray(object === null || object === void 0 ? void 0 : object.participants)
                ? object.participants.map((e) => exports.KeygenParticipant.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        var _a;
        const obj = {};
        if (!message.startedAt.equals(long_1.default.ZERO)) {
            obj.startedAt = (message.startedAt || long_1.default.ZERO).toString();
        }
        if (message.startedAtTimestamp !== undefined) {
            obj.startedAtTimestamp = fromTimestamp(message.startedAtTimestamp).toISOString();
        }
        if (!message.expiresAt.equals(long_1.default.ZERO)) {
            obj.expiresAt = (message.expiresAt || long_1.default.ZERO).toString();
        }
        if (!message.completedAt.equals(long_1.default.ZERO)) {
            obj.completedAt = (message.completedAt || long_1.default.ZERO).toString();
        }
        if (!message.gracePeriod.equals(long_1.default.ZERO)) {
            obj.gracePeriod = (message.gracePeriod || long_1.default.ZERO).toString();
        }
        if (message.state !== 0) {
            obj.state = (0, types_1.multisigStateToJSON)(message.state);
        }
        if (message.keygenThresholdWeight.length !== 0) {
            obj.keygenThresholdWeight = base64FromBytes(message.keygenThresholdWeight);
        }
        if (message.signingThresholdWeight.length !== 0) {
            obj.signingThresholdWeight = base64FromBytes(message.signingThresholdWeight);
        }
        if (message.bondedWeight.length !== 0) {
            obj.bondedWeight = base64FromBytes(message.bondedWeight);
        }
        if ((_a = message.participants) === null || _a === void 0 ? void 0 : _a.length) {
            obj.participants = message.participants.map((e) => exports.KeygenParticipant.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.KeygenSessionResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBaseKeygenSessionResponse();
        message.startedAt =
            object.startedAt !== undefined && object.startedAt !== null
                ? long_1.default.fromValue(object.startedAt)
                : long_1.default.ZERO;
        message.startedAtTimestamp =
            object.startedAtTimestamp !== undefined && object.startedAtTimestamp !== null
                ? timestamp_1.Timestamp.fromPartial(object.startedAtTimestamp)
                : undefined;
        message.expiresAt =
            object.expiresAt !== undefined && object.expiresAt !== null
                ? long_1.default.fromValue(object.expiresAt)
                : long_1.default.ZERO;
        message.completedAt =
            object.completedAt !== undefined && object.completedAt !== null
                ? long_1.default.fromValue(object.completedAt)
                : long_1.default.ZERO;
        message.gracePeriod =
            object.gracePeriod !== undefined && object.gracePeriod !== null
                ? long_1.default.fromValue(object.gracePeriod)
                : long_1.default.ZERO;
        message.state = (_a = object.state) !== null && _a !== void 0 ? _a : 0;
        message.keygenThresholdWeight = (_b = object.keygenThresholdWeight) !== null && _b !== void 0 ? _b : Buffer.alloc(0);
        message.signingThresholdWeight = (_c = object.signingThresholdWeight) !== null && _c !== void 0 ? _c : Buffer.alloc(0);
        message.bondedWeight = (_d = object.bondedWeight) !== null && _d !== void 0 ? _d : Buffer.alloc(0);
        message.participants = ((_e = object.participants) === null || _e === void 0 ? void 0 : _e.map((e) => exports.KeygenParticipant.fromPartial(e))) || [];
        return message;
    },
};
function createBaseParamsRequest() {
    return {};
}
exports.ParamsRequest = {
    encode(_, writer = minimal_1.default.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseParamsRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.ParamsRequest.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(_) {
        const message = createBaseParamsRequest();
        return message;
    },
};
function createBaseParamsResponse() {
    return { params: undefined };
}
exports.ParamsResponse = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.params !== undefined) {
            params_1.Params.encode(message.params, writer.uint32(10).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseParamsResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.params = params_1.Params.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { params: isSet(object.params) ? params_1.Params.fromJSON(object.params) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.params !== undefined) {
            obj.params = params_1.Params.toJSON(message.params);
        }
        return obj;
    },
    create(base) {
        return exports.ParamsResponse.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        const message = createBaseParamsResponse();
        message.params =
            object.params !== undefined && object.params !== null ? params_1.Params.fromPartial(object.params) : undefined;
        return message;
    },
};
const gt = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    return Uint8Array.from(gt.Buffer.from(b64, "base64"));
}
function base64FromBytes(arr) {
    return gt.Buffer.from(arr).toString("base64");
}
function toTimestamp(date) {
    const seconds = numberToLong(Math.trunc(date.getTime() / 1000));
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds.toNumber() || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new gt.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof gt.Date) {
        return toTimestamp(o);
    }
    else if (typeof o === "string") {
        return toTimestamp(new gt.Date(o));
    }
    else {
        return timestamp_1.Timestamp.fromJSON(o);
    }
}
function numberToLong(number) {
    return long_1.default.fromNumber(number);
}
if (minimal_1.default.util.Long !== long_1.default) {
    minimal_1.default.util.Long = long_1.default;
    minimal_1.default.configure();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=query.js.map