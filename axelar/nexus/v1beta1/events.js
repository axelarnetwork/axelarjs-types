"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v3.21.12
// source: axelar/nexus/v1beta1/events.proto
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WasmMessageRouted = exports.MessageFailed = exports.MessageExecuted = exports.MessageProcessing = exports.MessageReceived = exports.RateLimitUpdated = exports.InsufficientFee = exports.FeeDeducted = exports.protobufPackage = void 0;
/* eslint-disable */
const long_1 = __importDefault(require("long"));
const minimal_1 = __importDefault(require("protobufjs/minimal"));
const coin_1 = require("../../../cosmos/base/v1beta1/coin");
const duration_1 = require("../../../google/protobuf/duration");
const typeRegistry_1 = require("../../../typeRegistry");
const types_1 = require("../exported/v1beta1/types");
exports.protobufPackage = "axelar.nexus.v1beta1";
function createBaseFeeDeducted() {
    return {
        $type: "axelar.nexus.v1beta1.FeeDeducted",
        transferId: long_1.default.UZERO,
        recipientChain: "",
        recipientAddress: "",
        amount: undefined,
        fee: undefined,
    };
}
exports.FeeDeducted = {
    $type: "axelar.nexus.v1beta1.FeeDeducted",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (!message.transferId.equals(long_1.default.UZERO)) {
            writer.uint32(8).uint64(message.transferId);
        }
        if (message.recipientChain !== "") {
            writer.uint32(18).string(message.recipientChain);
        }
        if (message.recipientAddress !== "") {
            writer.uint32(26).string(message.recipientAddress);
        }
        if (message.amount !== undefined) {
            coin_1.Coin.encode(message.amount, writer.uint32(34).fork()).ldelim();
        }
        if (message.fee !== undefined) {
            coin_1.Coin.encode(message.fee, writer.uint32(42).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFeeDeducted();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.transferId = reader.uint64();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.recipientChain = reader.string();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.recipientAddress = reader.string();
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.amount = coin_1.Coin.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.fee = coin_1.Coin.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.FeeDeducted.$type,
            transferId: isSet(object.transferId) ? long_1.default.fromValue(object.transferId) : long_1.default.UZERO,
            recipientChain: isSet(object.recipientChain) ? gt.String(object.recipientChain) : "",
            recipientAddress: isSet(object.recipientAddress) ? gt.String(object.recipientAddress) : "",
            amount: isSet(object.amount) ? coin_1.Coin.fromJSON(object.amount) : undefined,
            fee: isSet(object.fee) ? coin_1.Coin.fromJSON(object.fee) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (!message.transferId.equals(long_1.default.UZERO)) {
            obj.transferId = (message.transferId || long_1.default.UZERO).toString();
        }
        if (message.recipientChain !== "") {
            obj.recipientChain = message.recipientChain;
        }
        if (message.recipientAddress !== "") {
            obj.recipientAddress = message.recipientAddress;
        }
        if (message.amount !== undefined) {
            obj.amount = coin_1.Coin.toJSON(message.amount);
        }
        if (message.fee !== undefined) {
            obj.fee = coin_1.Coin.toJSON(message.fee);
        }
        return obj;
    },
    create(base) {
        return exports.FeeDeducted.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseFeeDeducted();
        message.transferId =
            object.transferId !== undefined && object.transferId !== null
                ? long_1.default.fromValue(object.transferId)
                : long_1.default.UZERO;
        message.recipientChain = (_a = object.recipientChain) !== null && _a !== void 0 ? _a : "";
        message.recipientAddress = (_b = object.recipientAddress) !== null && _b !== void 0 ? _b : "";
        message.amount =
            object.amount !== undefined && object.amount !== null ? coin_1.Coin.fromPartial(object.amount) : undefined;
        message.fee = object.fee !== undefined && object.fee !== null ? coin_1.Coin.fromPartial(object.fee) : undefined;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.FeeDeducted.$type, exports.FeeDeducted);
function createBaseInsufficientFee() {
    return {
        $type: "axelar.nexus.v1beta1.InsufficientFee",
        transferId: long_1.default.UZERO,
        recipientChain: "",
        recipientAddress: "",
        amount: undefined,
        fee: undefined,
    };
}
exports.InsufficientFee = {
    $type: "axelar.nexus.v1beta1.InsufficientFee",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (!message.transferId.equals(long_1.default.UZERO)) {
            writer.uint32(8).uint64(message.transferId);
        }
        if (message.recipientChain !== "") {
            writer.uint32(18).string(message.recipientChain);
        }
        if (message.recipientAddress !== "") {
            writer.uint32(26).string(message.recipientAddress);
        }
        if (message.amount !== undefined) {
            coin_1.Coin.encode(message.amount, writer.uint32(34).fork()).ldelim();
        }
        if (message.fee !== undefined) {
            coin_1.Coin.encode(message.fee, writer.uint32(42).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseInsufficientFee();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.transferId = reader.uint64();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.recipientChain = reader.string();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.recipientAddress = reader.string();
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.amount = coin_1.Coin.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.fee = coin_1.Coin.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.InsufficientFee.$type,
            transferId: isSet(object.transferId) ? long_1.default.fromValue(object.transferId) : long_1.default.UZERO,
            recipientChain: isSet(object.recipientChain) ? gt.String(object.recipientChain) : "",
            recipientAddress: isSet(object.recipientAddress) ? gt.String(object.recipientAddress) : "",
            amount: isSet(object.amount) ? coin_1.Coin.fromJSON(object.amount) : undefined,
            fee: isSet(object.fee) ? coin_1.Coin.fromJSON(object.fee) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (!message.transferId.equals(long_1.default.UZERO)) {
            obj.transferId = (message.transferId || long_1.default.UZERO).toString();
        }
        if (message.recipientChain !== "") {
            obj.recipientChain = message.recipientChain;
        }
        if (message.recipientAddress !== "") {
            obj.recipientAddress = message.recipientAddress;
        }
        if (message.amount !== undefined) {
            obj.amount = coin_1.Coin.toJSON(message.amount);
        }
        if (message.fee !== undefined) {
            obj.fee = coin_1.Coin.toJSON(message.fee);
        }
        return obj;
    },
    create(base) {
        return exports.InsufficientFee.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseInsufficientFee();
        message.transferId =
            object.transferId !== undefined && object.transferId !== null
                ? long_1.default.fromValue(object.transferId)
                : long_1.default.UZERO;
        message.recipientChain = (_a = object.recipientChain) !== null && _a !== void 0 ? _a : "";
        message.recipientAddress = (_b = object.recipientAddress) !== null && _b !== void 0 ? _b : "";
        message.amount =
            object.amount !== undefined && object.amount !== null ? coin_1.Coin.fromPartial(object.amount) : undefined;
        message.fee = object.fee !== undefined && object.fee !== null ? coin_1.Coin.fromPartial(object.fee) : undefined;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.InsufficientFee.$type, exports.InsufficientFee);
function createBaseRateLimitUpdated() {
    return { $type: "axelar.nexus.v1beta1.RateLimitUpdated", chain: "", limit: undefined, window: undefined };
}
exports.RateLimitUpdated = {
    $type: "axelar.nexus.v1beta1.RateLimitUpdated",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.chain !== "") {
            writer.uint32(10).string(message.chain);
        }
        if (message.limit !== undefined) {
            coin_1.Coin.encode(message.limit, writer.uint32(18).fork()).ldelim();
        }
        if (message.window !== undefined) {
            duration_1.Duration.encode(message.window, writer.uint32(26).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRateLimitUpdated();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.chain = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.limit = coin_1.Coin.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.window = duration_1.Duration.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.RateLimitUpdated.$type,
            chain: isSet(object.chain) ? gt.String(object.chain) : "",
            limit: isSet(object.limit) ? coin_1.Coin.fromJSON(object.limit) : undefined,
            window: isSet(object.window) ? duration_1.Duration.fromJSON(object.window) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.chain !== "") {
            obj.chain = message.chain;
        }
        if (message.limit !== undefined) {
            obj.limit = coin_1.Coin.toJSON(message.limit);
        }
        if (message.window !== undefined) {
            obj.window = duration_1.Duration.toJSON(message.window);
        }
        return obj;
    },
    create(base) {
        return exports.RateLimitUpdated.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseRateLimitUpdated();
        message.chain = (_a = object.chain) !== null && _a !== void 0 ? _a : "";
        message.limit =
            object.limit !== undefined && object.limit !== null ? coin_1.Coin.fromPartial(object.limit) : undefined;
        message.window =
            object.window !== undefined && object.window !== null ? duration_1.Duration.fromPartial(object.window) : undefined;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.RateLimitUpdated.$type, exports.RateLimitUpdated);
function createBaseMessageReceived() {
    return {
        $type: "axelar.nexus.v1beta1.MessageReceived",
        id: "",
        payloadHash: Buffer.alloc(0),
        sender: undefined,
        recipient: undefined,
    };
}
exports.MessageReceived = {
    $type: "axelar.nexus.v1beta1.MessageReceived",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.payloadHash.length !== 0) {
            writer.uint32(18).bytes(message.payloadHash);
        }
        if (message.sender !== undefined) {
            types_1.CrossChainAddress.encode(message.sender, writer.uint32(26).fork()).ldelim();
        }
        if (message.recipient !== undefined) {
            types_1.CrossChainAddress.encode(message.recipient, writer.uint32(34).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMessageReceived();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.payloadHash = reader.bytes();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.sender = types_1.CrossChainAddress.decode(reader, reader.uint32());
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.recipient = types_1.CrossChainAddress.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.MessageReceived.$type,
            id: isSet(object.id) ? gt.String(object.id) : "",
            payloadHash: isSet(object.payloadHash)
                ? Buffer.from(bytesFromBase64(object.payloadHash))
                : Buffer.alloc(0),
            sender: isSet(object.sender) ? types_1.CrossChainAddress.fromJSON(object.sender) : undefined,
            recipient: isSet(object.recipient) ? types_1.CrossChainAddress.fromJSON(object.recipient) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.payloadHash.length !== 0) {
            obj.payloadHash = base64FromBytes(message.payloadHash);
        }
        if (message.sender !== undefined) {
            obj.sender = types_1.CrossChainAddress.toJSON(message.sender);
        }
        if (message.recipient !== undefined) {
            obj.recipient = types_1.CrossChainAddress.toJSON(message.recipient);
        }
        return obj;
    },
    create(base) {
        return exports.MessageReceived.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseMessageReceived();
        message.id = (_a = object.id) !== null && _a !== void 0 ? _a : "";
        message.payloadHash = (_b = object.payloadHash) !== null && _b !== void 0 ? _b : Buffer.alloc(0);
        message.sender =
            object.sender !== undefined && object.sender !== null
                ? types_1.CrossChainAddress.fromPartial(object.sender)
                : undefined;
        message.recipient =
            object.recipient !== undefined && object.recipient !== null
                ? types_1.CrossChainAddress.fromPartial(object.recipient)
                : undefined;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.MessageReceived.$type, exports.MessageReceived);
function createBaseMessageProcessing() {
    return { $type: "axelar.nexus.v1beta1.MessageProcessing", id: "", sourceChain: "", destinationChain: "" };
}
exports.MessageProcessing = {
    $type: "axelar.nexus.v1beta1.MessageProcessing",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.sourceChain !== "") {
            writer.uint32(18).string(message.sourceChain);
        }
        if (message.destinationChain !== "") {
            writer.uint32(26).string(message.destinationChain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMessageProcessing();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.sourceChain = reader.string();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.destinationChain = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.MessageProcessing.$type,
            id: isSet(object.id) ? gt.String(object.id) : "",
            sourceChain: isSet(object.sourceChain) ? gt.String(object.sourceChain) : "",
            destinationChain: isSet(object.destinationChain) ? gt.String(object.destinationChain) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.sourceChain !== "") {
            obj.sourceChain = message.sourceChain;
        }
        if (message.destinationChain !== "") {
            obj.destinationChain = message.destinationChain;
        }
        return obj;
    },
    create(base) {
        return exports.MessageProcessing.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseMessageProcessing();
        message.id = (_a = object.id) !== null && _a !== void 0 ? _a : "";
        message.sourceChain = (_b = object.sourceChain) !== null && _b !== void 0 ? _b : "";
        message.destinationChain = (_c = object.destinationChain) !== null && _c !== void 0 ? _c : "";
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.MessageProcessing.$type, exports.MessageProcessing);
function createBaseMessageExecuted() {
    return { $type: "axelar.nexus.v1beta1.MessageExecuted", id: "", sourceChain: "", destinationChain: "" };
}
exports.MessageExecuted = {
    $type: "axelar.nexus.v1beta1.MessageExecuted",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.sourceChain !== "") {
            writer.uint32(18).string(message.sourceChain);
        }
        if (message.destinationChain !== "") {
            writer.uint32(26).string(message.destinationChain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMessageExecuted();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.sourceChain = reader.string();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.destinationChain = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.MessageExecuted.$type,
            id: isSet(object.id) ? gt.String(object.id) : "",
            sourceChain: isSet(object.sourceChain) ? gt.String(object.sourceChain) : "",
            destinationChain: isSet(object.destinationChain) ? gt.String(object.destinationChain) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.sourceChain !== "") {
            obj.sourceChain = message.sourceChain;
        }
        if (message.destinationChain !== "") {
            obj.destinationChain = message.destinationChain;
        }
        return obj;
    },
    create(base) {
        return exports.MessageExecuted.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseMessageExecuted();
        message.id = (_a = object.id) !== null && _a !== void 0 ? _a : "";
        message.sourceChain = (_b = object.sourceChain) !== null && _b !== void 0 ? _b : "";
        message.destinationChain = (_c = object.destinationChain) !== null && _c !== void 0 ? _c : "";
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.MessageExecuted.$type, exports.MessageExecuted);
function createBaseMessageFailed() {
    return { $type: "axelar.nexus.v1beta1.MessageFailed", id: "", sourceChain: "", destinationChain: "" };
}
exports.MessageFailed = {
    $type: "axelar.nexus.v1beta1.MessageFailed",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.sourceChain !== "") {
            writer.uint32(18).string(message.sourceChain);
        }
        if (message.destinationChain !== "") {
            writer.uint32(26).string(message.destinationChain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMessageFailed();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.sourceChain = reader.string();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.destinationChain = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.MessageFailed.$type,
            id: isSet(object.id) ? gt.String(object.id) : "",
            sourceChain: isSet(object.sourceChain) ? gt.String(object.sourceChain) : "",
            destinationChain: isSet(object.destinationChain) ? gt.String(object.destinationChain) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.sourceChain !== "") {
            obj.sourceChain = message.sourceChain;
        }
        if (message.destinationChain !== "") {
            obj.destinationChain = message.destinationChain;
        }
        return obj;
    },
    create(base) {
        return exports.MessageFailed.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseMessageFailed();
        message.id = (_a = object.id) !== null && _a !== void 0 ? _a : "";
        message.sourceChain = (_b = object.sourceChain) !== null && _b !== void 0 ? _b : "";
        message.destinationChain = (_c = object.destinationChain) !== null && _c !== void 0 ? _c : "";
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.MessageFailed.$type, exports.MessageFailed);
function createBaseWasmMessageRouted() {
    return { $type: "axelar.nexus.v1beta1.WasmMessageRouted", message: undefined };
}
exports.WasmMessageRouted = {
    $type: "axelar.nexus.v1beta1.WasmMessageRouted",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.message !== undefined) {
            types_1.WasmMessage.encode(message.message, writer.uint32(10).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseWasmMessageRouted();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.message = types_1.WasmMessage.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.WasmMessageRouted.$type,
            message: isSet(object.message) ? types_1.WasmMessage.fromJSON(object.message) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.message !== undefined) {
            obj.message = types_1.WasmMessage.toJSON(message.message);
        }
        return obj;
    },
    create(base) {
        return exports.WasmMessageRouted.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        const message = createBaseWasmMessageRouted();
        message.message =
            object.message !== undefined && object.message !== null
                ? types_1.WasmMessage.fromPartial(object.message)
                : undefined;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.WasmMessageRouted.$type, exports.WasmMessageRouted);
const gt = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    return Uint8Array.from(gt.Buffer.from(b64, "base64"));
}
function base64FromBytes(arr) {
    return gt.Buffer.from(arr).toString("base64");
}
if (minimal_1.default.util.Long !== long_1.default) {
    minimal_1.default.util.Long = long_1.default;
    minimal_1.default.configure();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=events.js.map