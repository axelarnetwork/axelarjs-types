"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.31.1
// source: axelar/nexus/v1beta1/types.proto
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TransferEpoch = exports.RateLimit = exports.LinkedAddresses = exports.ChainState = exports.MaintainerState = exports.protobufPackage = void 0;
/* eslint-disable */
const long_1 = __importDefault(require("long"));
const minimal_1 = __importDefault(require("protobufjs/minimal"));
const coin_1 = require("../../../cosmos/base/v1beta1/coin");
const duration_1 = require("../../../google/protobuf/duration");
const typeRegistry_1 = require("../../../typeRegistry");
const bitmap_1 = require("../../utils/v1beta1/bitmap");
const types_1 = require("../exported/v1beta1/types");
exports.protobufPackage = "axelar.nexus.v1beta1";
function createBaseMaintainerState() {
    return {
        $type: "axelar.nexus.v1beta1.MaintainerState",
        address: Buffer.alloc(0),
        missingVotes: undefined,
        incorrectVotes: undefined,
        chain: "",
    };
}
exports.MaintainerState = {
    $type: "axelar.nexus.v1beta1.MaintainerState",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.address.length !== 0) {
            writer.uint32(10).bytes(message.address);
        }
        if (message.missingVotes !== undefined) {
            bitmap_1.Bitmap.encode(message.missingVotes, writer.uint32(18).fork()).ldelim();
        }
        if (message.incorrectVotes !== undefined) {
            bitmap_1.Bitmap.encode(message.incorrectVotes, writer.uint32(26).fork()).ldelim();
        }
        if (message.chain !== "") {
            writer.uint32(34).string(message.chain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMaintainerState();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.address = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.missingVotes = bitmap_1.Bitmap.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.incorrectVotes = bitmap_1.Bitmap.decode(reader, reader.uint32());
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.chain = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.MaintainerState.$type,
            address: isSet(object.address) ? Buffer.from(bytesFromBase64(object.address)) : Buffer.alloc(0),
            missingVotes: isSet(object.missingVotes) ? bitmap_1.Bitmap.fromJSON(object.missingVotes) : undefined,
            incorrectVotes: isSet(object.incorrectVotes) ? bitmap_1.Bitmap.fromJSON(object.incorrectVotes) : undefined,
            chain: isSet(object.chain) ? gt.String(object.chain) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.address.length !== 0) {
            obj.address = base64FromBytes(message.address);
        }
        if (message.missingVotes !== undefined) {
            obj.missingVotes = bitmap_1.Bitmap.toJSON(message.missingVotes);
        }
        if (message.incorrectVotes !== undefined) {
            obj.incorrectVotes = bitmap_1.Bitmap.toJSON(message.incorrectVotes);
        }
        if (message.chain !== "") {
            obj.chain = message.chain;
        }
        return obj;
    },
    create(base) {
        return exports.MaintainerState.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseMaintainerState();
        message.address = (_a = object.address) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.missingVotes =
            object.missingVotes !== undefined && object.missingVotes !== null
                ? bitmap_1.Bitmap.fromPartial(object.missingVotes)
                : undefined;
        message.incorrectVotes =
            object.incorrectVotes !== undefined && object.incorrectVotes !== null
                ? bitmap_1.Bitmap.fromPartial(object.incorrectVotes)
                : undefined;
        message.chain = (_b = object.chain) !== null && _b !== void 0 ? _b : "";
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.MaintainerState.$type, exports.MaintainerState);
function createBaseChainState() {
    return {
        $type: "axelar.nexus.v1beta1.ChainState",
        chain: undefined,
        activated: false,
        assets: [],
        maintainerStates: [],
    };
}
exports.ChainState = {
    $type: "axelar.nexus.v1beta1.ChainState",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.chain !== undefined) {
            types_1.Chain.encode(message.chain, writer.uint32(10).fork()).ldelim();
        }
        if (message.activated !== false) {
            writer.uint32(24).bool(message.activated);
        }
        for (const v of message.assets) {
            types_1.Asset.encode(v, writer.uint32(42).fork()).ldelim();
        }
        for (const v of message.maintainerStates) {
            exports.MaintainerState.encode(v, writer.uint32(50).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseChainState();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.chain = types_1.Chain.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.activated = reader.bool();
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.assets.push(types_1.Asset.decode(reader, reader.uint32()));
                    continue;
                case 6:
                    if (tag !== 50) {
                        break;
                    }
                    message.maintainerStates.push(exports.MaintainerState.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.ChainState.$type,
            chain: isSet(object.chain) ? types_1.Chain.fromJSON(object.chain) : undefined,
            activated: isSet(object.activated) ? gt.Boolean(object.activated) : false,
            assets: gt.Array.isArray(object === null || object === void 0 ? void 0 : object.assets) ? object.assets.map((e) => types_1.Asset.fromJSON(e)) : [],
            maintainerStates: gt.Array.isArray(object === null || object === void 0 ? void 0 : object.maintainerStates)
                ? object.maintainerStates.map((e) => exports.MaintainerState.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        var _a, _b;
        const obj = {};
        if (message.chain !== undefined) {
            obj.chain = types_1.Chain.toJSON(message.chain);
        }
        if (message.activated !== false) {
            obj.activated = message.activated;
        }
        if ((_a = message.assets) === null || _a === void 0 ? void 0 : _a.length) {
            obj.assets = message.assets.map((e) => types_1.Asset.toJSON(e));
        }
        if ((_b = message.maintainerStates) === null || _b === void 0 ? void 0 : _b.length) {
            obj.maintainerStates = message.maintainerStates.map((e) => exports.MaintainerState.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.ChainState.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseChainState();
        message.chain =
            object.chain !== undefined && object.chain !== null ? types_1.Chain.fromPartial(object.chain) : undefined;
        message.activated = (_a = object.activated) !== null && _a !== void 0 ? _a : false;
        message.assets = ((_b = object.assets) === null || _b === void 0 ? void 0 : _b.map((e) => types_1.Asset.fromPartial(e))) || [];
        message.maintainerStates = ((_c = object.maintainerStates) === null || _c === void 0 ? void 0 : _c.map((e) => exports.MaintainerState.fromPartial(e))) || [];
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.ChainState.$type, exports.ChainState);
function createBaseLinkedAddresses() {
    return {
        $type: "axelar.nexus.v1beta1.LinkedAddresses",
        depositAddress: undefined,
        recipientAddress: undefined,
    };
}
exports.LinkedAddresses = {
    $type: "axelar.nexus.v1beta1.LinkedAddresses",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.depositAddress !== undefined) {
            types_1.CrossChainAddress.encode(message.depositAddress, writer.uint32(10).fork()).ldelim();
        }
        if (message.recipientAddress !== undefined) {
            types_1.CrossChainAddress.encode(message.recipientAddress, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseLinkedAddresses();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.depositAddress = types_1.CrossChainAddress.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.recipientAddress = types_1.CrossChainAddress.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.LinkedAddresses.$type,
            depositAddress: isSet(object.depositAddress)
                ? types_1.CrossChainAddress.fromJSON(object.depositAddress)
                : undefined,
            recipientAddress: isSet(object.recipientAddress)
                ? types_1.CrossChainAddress.fromJSON(object.recipientAddress)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.depositAddress !== undefined) {
            obj.depositAddress = types_1.CrossChainAddress.toJSON(message.depositAddress);
        }
        if (message.recipientAddress !== undefined) {
            obj.recipientAddress = types_1.CrossChainAddress.toJSON(message.recipientAddress);
        }
        return obj;
    },
    create(base) {
        return exports.LinkedAddresses.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        const message = createBaseLinkedAddresses();
        message.depositAddress =
            object.depositAddress !== undefined && object.depositAddress !== null
                ? types_1.CrossChainAddress.fromPartial(object.depositAddress)
                : undefined;
        message.recipientAddress =
            object.recipientAddress !== undefined && object.recipientAddress !== null
                ? types_1.CrossChainAddress.fromPartial(object.recipientAddress)
                : undefined;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.LinkedAddresses.$type, exports.LinkedAddresses);
function createBaseRateLimit() {
    return { $type: "axelar.nexus.v1beta1.RateLimit", chain: "", limit: undefined, window: undefined };
}
exports.RateLimit = {
    $type: "axelar.nexus.v1beta1.RateLimit",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.chain !== "") {
            writer.uint32(10).string(message.chain);
        }
        if (message.limit !== undefined) {
            coin_1.Coin.encode(message.limit, writer.uint32(18).fork()).ldelim();
        }
        if (message.window !== undefined) {
            duration_1.Duration.encode(message.window, writer.uint32(26).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRateLimit();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.chain = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.limit = coin_1.Coin.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.window = duration_1.Duration.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.RateLimit.$type,
            chain: isSet(object.chain) ? gt.String(object.chain) : "",
            limit: isSet(object.limit) ? coin_1.Coin.fromJSON(object.limit) : undefined,
            window: isSet(object.window) ? duration_1.Duration.fromJSON(object.window) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.chain !== "") {
            obj.chain = message.chain;
        }
        if (message.limit !== undefined) {
            obj.limit = coin_1.Coin.toJSON(message.limit);
        }
        if (message.window !== undefined) {
            obj.window = duration_1.Duration.toJSON(message.window);
        }
        return obj;
    },
    create(base) {
        return exports.RateLimit.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseRateLimit();
        message.chain = (_a = object.chain) !== null && _a !== void 0 ? _a : "";
        message.limit =
            object.limit !== undefined && object.limit !== null ? coin_1.Coin.fromPartial(object.limit) : undefined;
        message.window =
            object.window !== undefined && object.window !== null ? duration_1.Duration.fromPartial(object.window) : undefined;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.RateLimit.$type, exports.RateLimit);
function createBaseTransferEpoch() {
    return {
        $type: "axelar.nexus.v1beta1.TransferEpoch",
        chain: "",
        amount: undefined,
        epoch: long_1.default.UZERO,
        direction: 0,
    };
}
exports.TransferEpoch = {
    $type: "axelar.nexus.v1beta1.TransferEpoch",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.chain !== "") {
            writer.uint32(10).string(message.chain);
        }
        if (message.amount !== undefined) {
            coin_1.Coin.encode(message.amount, writer.uint32(18).fork()).ldelim();
        }
        if (!message.epoch.equals(long_1.default.UZERO)) {
            writer.uint32(24).uint64(message.epoch);
        }
        if (message.direction !== 0) {
            writer.uint32(32).int32(message.direction);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTransferEpoch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.chain = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.amount = coin_1.Coin.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.epoch = reader.uint64();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.direction = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.TransferEpoch.$type,
            chain: isSet(object.chain) ? gt.String(object.chain) : "",
            amount: isSet(object.amount) ? coin_1.Coin.fromJSON(object.amount) : undefined,
            epoch: isSet(object.epoch) ? long_1.default.fromValue(object.epoch) : long_1.default.UZERO,
            direction: isSet(object.direction) ? (0, types_1.transferDirectionFromJSON)(object.direction) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.chain !== "") {
            obj.chain = message.chain;
        }
        if (message.amount !== undefined) {
            obj.amount = coin_1.Coin.toJSON(message.amount);
        }
        if (!message.epoch.equals(long_1.default.UZERO)) {
            obj.epoch = (message.epoch || long_1.default.UZERO).toString();
        }
        if (message.direction !== 0) {
            obj.direction = (0, types_1.transferDirectionToJSON)(message.direction);
        }
        return obj;
    },
    create(base) {
        return exports.TransferEpoch.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseTransferEpoch();
        message.chain = (_a = object.chain) !== null && _a !== void 0 ? _a : "";
        message.amount =
            object.amount !== undefined && object.amount !== null ? coin_1.Coin.fromPartial(object.amount) : undefined;
        message.epoch =
            object.epoch !== undefined && object.epoch !== null ? long_1.default.fromValue(object.epoch) : long_1.default.UZERO;
        message.direction = (_b = object.direction) !== null && _b !== void 0 ? _b : 0;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.TransferEpoch.$type, exports.TransferEpoch);
const gt = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    return Uint8Array.from(gt.Buffer.from(b64, "base64"));
}
function base64FromBytes(arr) {
    return gt.Buffer.from(arr).toString("base64");
}
if (minimal_1.default.util.Long !== long_1.default) {
    minimal_1.default.util.Long = long_1.default;
    minimal_1.default.configure();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=types.js.map