"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: axelar/vote/v1beta1/types.proto
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TalliedVote_IsVoterLateEntry = exports.TalliedVote = exports.protobufPackage = void 0;
/* eslint-disable */
const long_1 = __importDefault(require("long"));
const minimal_1 = __importDefault(require("protobufjs/minimal"));
const any_1 = require("../../../google/protobuf/any");
const typeRegistry_1 = require("../../../typeRegistry");
exports.protobufPackage = "axelar.vote.v1beta1";
function createBaseTalliedVote() {
    return {
        $type: "axelar.vote.v1beta1.TalliedVote",
        tally: Buffer.alloc(0),
        votersDeprecated: [],
        data: undefined,
        pollId: long_1.default.UZERO,
        isVoterLate: {},
    };
}
exports.TalliedVote = {
    $type: "axelar.vote.v1beta1.TalliedVote",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.tally.length !== 0) {
            writer.uint32(10).bytes(message.tally);
        }
        for (const v of message.votersDeprecated) {
            writer.uint32(18).bytes(v);
        }
        if (message.data !== undefined) {
            any_1.Any.encode(message.data, writer.uint32(26).fork()).ldelim();
        }
        if (!message.pollId.equals(long_1.default.UZERO)) {
            writer.uint32(32).uint64(message.pollId);
        }
        Object.entries(message.isVoterLate).forEach(([key, value]) => {
            exports.TalliedVote_IsVoterLateEntry.encode({
                $type: "axelar.vote.v1beta1.TalliedVote.IsVoterLateEntry",
                key: key,
                value,
            }, writer.uint32(42).fork()).ldelim();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTalliedVote();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.tally = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.votersDeprecated.push(reader.bytes());
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.data = any_1.Any.decode(reader, reader.uint32());
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.pollId = reader.uint64();
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    const entry5 = exports.TalliedVote_IsVoterLateEntry.decode(reader, reader.uint32());
                    if (entry5.value !== undefined) {
                        message.isVoterLate[entry5.key] = entry5.value;
                    }
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.TalliedVote.$type,
            tally: isSet(object.tally) ? Buffer.from(bytesFromBase64(object.tally)) : Buffer.alloc(0),
            votersDeprecated: gt.Array.isArray(object === null || object === void 0 ? void 0 : object.votersDeprecated)
                ? object.votersDeprecated.map((e) => Buffer.from(bytesFromBase64(e)))
                : [],
            data: isSet(object.data) ? any_1.Any.fromJSON(object.data) : undefined,
            pollId: isSet(object.pollId) ? long_1.default.fromValue(object.pollId) : long_1.default.UZERO,
            isVoterLate: isObject(object.isVoterLate)
                ? Object.entries(object.isVoterLate).reduce((acc, [key, value]) => {
                    acc[key] = Boolean(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        var _a;
        const obj = {};
        if (message.tally.length !== 0) {
            obj.tally = base64FromBytes(message.tally);
        }
        if ((_a = message.votersDeprecated) === null || _a === void 0 ? void 0 : _a.length) {
            obj.votersDeprecated = message.votersDeprecated.map((e) => base64FromBytes(e));
        }
        if (message.data !== undefined) {
            obj.data = any_1.Any.toJSON(message.data);
        }
        if (!message.pollId.equals(long_1.default.UZERO)) {
            obj.pollId = (message.pollId || long_1.default.UZERO).toString();
        }
        if (message.isVoterLate) {
            const entries = Object.entries(message.isVoterLate);
            if (entries.length > 0) {
                obj.isVoterLate = {};
                entries.forEach(([k, v]) => {
                    obj.isVoterLate[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.TalliedVote.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseTalliedVote();
        message.tally = (_a = object.tally) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.votersDeprecated = ((_b = object.votersDeprecated) === null || _b === void 0 ? void 0 : _b.map((e) => e)) || [];
        message.data =
            object.data !== undefined && object.data !== null ? any_1.Any.fromPartial(object.data) : undefined;
        message.pollId =
            object.pollId !== undefined && object.pollId !== null ? long_1.default.fromValue(object.pollId) : long_1.default.UZERO;
        message.isVoterLate = Object.entries((_c = object.isVoterLate) !== null && _c !== void 0 ? _c : {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = gt.Boolean(value);
            }
            return acc;
        }, {});
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.TalliedVote.$type, exports.TalliedVote);
function createBaseTalliedVote_IsVoterLateEntry() {
    return { $type: "axelar.vote.v1beta1.TalliedVote.IsVoterLateEntry", key: "", value: false };
}
exports.TalliedVote_IsVoterLateEntry = {
    $type: "axelar.vote.v1beta1.TalliedVote.IsVoterLateEntry",
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== false) {
            writer.uint32(16).bool(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTalliedVote_IsVoterLateEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.value = reader.bool();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            $type: exports.TalliedVote_IsVoterLateEntry.$type,
            key: isSet(object.key) ? gt.String(object.key) : "",
            value: isSet(object.value) ? gt.Boolean(object.value) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== false) {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return exports.TalliedVote_IsVoterLateEntry.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseTalliedVote_IsVoterLateEntry();
        message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
        message.value = (_b = object.value) !== null && _b !== void 0 ? _b : false;
        return message;
    },
};
typeRegistry_1.messageTypeRegistry.set(exports.TalliedVote_IsVoterLateEntry.$type, exports.TalliedVote_IsVoterLateEntry);
const gt = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    return Uint8Array.from(gt.Buffer.from(b64, "base64"));
}
function base64FromBytes(arr) {
    return gt.Buffer.from(arr).toString("base64");
}
if (minimal_1.default.util.Long !== long_1.default) {
    minimal_1.default.util.Long = long_1.default;
    minimal_1.default.configure();
}
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=types.js.map